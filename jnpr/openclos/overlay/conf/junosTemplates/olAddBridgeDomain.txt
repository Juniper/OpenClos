vlans {
    replace:
    bd{{vxlanId}} {
        vlan-id {{vlanId}};
        l3-interface irb.{{vlanId}};
        vxlan {
            vni {{vxlanId}};
            ingress-node-replication;
        }
    }
}