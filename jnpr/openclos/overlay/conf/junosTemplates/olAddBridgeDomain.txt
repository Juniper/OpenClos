vlans {
    replace:
    bd_{{networkName}} {
        {% if role == "spine" %}vlan-id {{vlanId}};
        l3-interface irb.{{vlanId}};{% endif %}
        vxlan {
            vni {{vxlanId}};
            ingress-node-replication;
        }
    }
}