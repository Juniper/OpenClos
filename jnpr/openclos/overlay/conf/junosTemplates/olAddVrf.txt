routing-instances {
    merge:
    VRF_{{vrfName}} {
        instance-type vrf;
        {% if vrfLoopbackName %}interface {{vrfLoopbackName}};{% endif %}
        {% if irbName %}interface {{irbName}};{% endif %}        
        {% if vni0 %}route-distinguisher {{routerId}}:{{vni0}};
        vrf-target target:{{asn}}:{{vni0}};{% endif %}
    }
}
