routing-instances {
    merge:
    {{vrfName}} {
        instance-type vrf;
        {% if vrfLoopbackName %}interface {{vrfLoopbackName}};{% endif %}
        {% if irbName %}interface {{irbName}};{% endif %}        
        {% if vnis | length > 0 %}route-distinguisher {{routerId}}:{{vnis[0]}};
        vrf-target target:{{asn}}:{{vnis[0]}};{% endif %}
    }
}