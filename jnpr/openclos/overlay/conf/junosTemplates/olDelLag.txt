{% if lagCount > 0 %}
chassis {
    aggregated-devices {
        ethernet {
            device-count {{lagCount}};
        }
    }
}
{% else %}
chassis {
    delete:
    aggregated-devices;
}
{% endif %}

interfaces {
    delete:
    {{interfaceName}};
    delete:
    {{lagName}};
}