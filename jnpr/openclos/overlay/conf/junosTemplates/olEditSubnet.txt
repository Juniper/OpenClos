interfaces {
    irb {
        unit {{vlanId}} {
            family inet {
                replace:
                address {{irbAddress}} {
                    virtual-gateway-address {{irbVirtualGateway}};
                }
                {% if oldIrbAddress %}
                delete:
                address {{oldIrbAddress}};
                {% endif %}
            }
        }
    }
}
