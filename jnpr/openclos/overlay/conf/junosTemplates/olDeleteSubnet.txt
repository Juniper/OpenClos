{% if role == "spine" %}
interfaces {
    irb {
        unit {{vlanId}} {
            family inet {
                delete:
                address {{irbAddress}};
            }
        }
    }
}

{% if subnetCount == 0 %}
vlans {
    {{networkName}} {
        delete:
        l3-interface irb.{{vlanId}};
    }
}

routing-instances {
    VRF_{{vrfName}} {
        delete:
        interface irb.{{vlanId}};    
    }
}
{% endif %}
{% endif %}
