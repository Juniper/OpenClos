protocols {
    evpn {
        encapsulation vxlan;
        multicast-mode ingress-replication;
        {% if vni %}extended-vni-list [ {{vni}} ];
        vni-options {
            vni {{vni}} {
                vrf-target export target:{{asn}}:{{vni}};
            }
        }{% endif %}
    }
}
