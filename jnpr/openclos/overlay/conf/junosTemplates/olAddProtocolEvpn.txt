protocols {
    evpn {
        encapsulation vxlan;
        extended-vni-list [ {{vni}} ];
        multicast-mode ingress-replication;
        vni-options {
            vni {{vni}} {
                vrf-target export target:{{asn}}:{{vni}};
            }
        }
    }
}
