routing-instances {
	replace:
    {{vrfName}} {
        instance-type vrf;
        interface {{vrfLoopbackName}};
        {% for irbName in irbs %}interface {{irbName}};
        {% endfor %}        
        {% if vnis | length > 0 %}route-distinguisher {{routerId}}:{{vnis[0]}};
        vrf-target target:{{asn}}:{{vnis[0]}};
        {% endif %}}
}